[build]
builder = "RAILPACK"
buildCommand = "npm run typecheck"

[deploy]
startCommand = "node --import tsx worker/continuous.ts"
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "ALWAYS"
